[package]
name = "uefi-async-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true  # 关键：开启过程宏支持

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"